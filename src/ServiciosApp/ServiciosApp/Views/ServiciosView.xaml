<Window x:Class="ServiciosApp.Views.ServiciosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiciosApp.Views"
        mc:Ignorable="d"
        Title="ServiciosView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Botones de acción -->
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    Margin="10">
            <Button Content="Nuevo Servicio" 
                    Width="120" 
                    Height="30"
                    Margin="0,0,10,0"
                    Click="BtnNuevoServicio_Click"/>
            <Button Content="Asignar Servicio" 
                    Width="120" 
                    Height="30"
                    Margin="0,0,10,0"
                    Click="BtnAsignarServicio_Click"/>
            <Button Content="Actualizar" 
                    Width="80" 
                    Height="30"
                    Click="BtnActualizar_Click"/>
        </StackPanel>

        <!-- Lista de servicios -->
        <DataGrid Grid.Row="1" 
                 Margin="10"
                 AutoGenerateColumns="False"
                 ItemsSource="{Binding Servicios}"
                 SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente.Nombre}" Width="150"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="200"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="100"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                <DataGridTextColumn Header="Fecha" 
                                   Binding="{Binding FechaSolicitud, StringFormat=dd/MM/yyyy}" 
                                   Width="100"/>
                <DataGridTextColumn Header="Costo" 
                                   Binding="{Binding Costo, StringFormat=C2}" 
                                   Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
